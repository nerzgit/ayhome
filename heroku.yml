build:
  docker:
    web: web/Dockerfile  # src/web フォルダ内の Dockerfile を指定

run:
  web: npm start --prefix web  # src/web フォルダ内で npm start を実行

release:
  command:
    - npm run migrate --prefix web  # データベースマイグレーションなどがあればここで実行
